# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr
server-dist
*.local

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# env & secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
service-account*.json
gcp-*.json
vertex-*.json
# secret store (default lives outside the repo; guard a repo-relative override)
secrets.json
*.secrets.json
secrets.json.tmp

# local backups (contain API keys) — NEVER commit
backups/
relay-backup-*.json

# local data store (the source-of-truth snapshot, contains API keys) — NEVER commit
data/
relay.json
relay.json.tmp
